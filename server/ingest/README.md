# server/ingest

**Purpose**: Document ingestion pipeline components for parsing and processing uploaded bank statements.

This directory contains modules responsible for:
- Document AI integration
- File format parsing (PDF, CSV, etc.)
- Transaction extraction and preprocessing
- Ingestion workflow orchestration
