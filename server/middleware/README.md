# server/middleware

**Purpose**: HTTP middleware components for request processing, authentication, validation, and error handling.

This directory contains reusable middleware functions that intercept and process incoming requests before they reach route handlers.
