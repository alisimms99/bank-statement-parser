# client/src/components/ingestion

**Purpose**: React components for the document ingestion user interface.

This directory contains UI components that handle file upload, ingestion progress tracking, validation feedback, and user interactions related to bank statement document processing.
